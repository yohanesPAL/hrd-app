/**
 * Database error codes used for handling specific DB-related failures.
 *
 * @property {string} duplicate - Error code for duplicate entry (e.g., unique constraint violation).
 */
export const dbErr = {
  duplicate: "ER_DUP_ENTRY",
};